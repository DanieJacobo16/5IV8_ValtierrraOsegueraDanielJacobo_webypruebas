<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bitácora de Actividades</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>📋 Bitácora de Actividades</h1>
            <% if (mensaje) { %>
                <div class="mensaje-exito">
                    <%= mensaje %>
                </div>
            <% } %>
        </header>

        <section class="form-section">
            <h2>Agregar Nueva Actividad</h2>
            <form action="/actividades" method="POST" class="actividad-form">
                <div class="form-row">
                    <div class="form-group">
                        <label for="fecha">Fecha:</label>
                        <input type="date" id="fecha" name="fecha" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="hora">Hora:</label>
                        <input type="time" id="hora" name="hora" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="prioridad">Prioridad:</label>
                        <select id="prioridad" name="prioridad" required>
                            <option value="">Seleccionar...</option>
                            <option value="Alta">Alta</option>
                            <option value="Media">Media</option>
                            <option value="Baja">Baja</option>
                        </select>
                    </div>
                </div>

                <div class="form-group">
                    <label for="actividad">Actividad:</label>
                    <input type="text" id="actividad" name="actividad" placeholder="Descripción breve de la actividad" required minlength="3">
                </div>

                <div class="form-group">
                    <label for="descripcion">Descripción:</label>
                    <textarea id="descripcion" name="descripcion" placeholder="Detalles de la actividad..." required></textarea>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="estado">Estado:</label>
                        <select id="estado" name="estado" required>
                            <option value="">Seleccionar...</option>
                            <option value="Pendiente">Pendiente</option>
                            <option value="En Progreso">En Progreso</option>
                            <option value="Completada">Completada</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="horas_invertidas">Horas Invertidas:</label>
                        <input type="number" id="horas_invertidas" name="horas_invertidas" step="0.5" min="0" max="24" required>
                    </div>
                </div>

                <button type="submit" class="btn-primary">Registrar Actividad</button>
            </form>
        </section>

        <section class="list-section">
            <h2>Actividades Registradas</h2>
            
            <% if (actividades.length === 0) { %>
                <p class="sin-actividades">No hay actividades registradas.</p>
            <% } else { %>
                <div class="actividades-grid">
                    <% actividades.forEach(function(actividad){ %>
                        <div class="actividad-card prioridad-<%= actividad.prioridad.toLowerCase() %> estado-<%= actividad.estado.toLowerCase().replace(' ', '-') %>">
                            <div class="actividad-header">
                                <h3><%= actividad.actividad %></h3>
                                <span class="prioridad-badge"><%= actividad.prioridad %></span>
                            </div>
                            
                            <div class="actividad-details">
                                <p><strong>📅 Fecha:</strong> <%= new Date(actividad.fecha).toLocaleDateString('es-ES') %></p>
                                <p><strong>⏰ Hora:</strong> <%= actividad.hora.substring(0,5) %></p>
                                <p><strong>📝 Descripción:</strong> <%= actividad.descripcion %></p>
                                <p><strong>🔄 Estado:</strong> <span class="estado-badge"><%= actividad.estado %></span></p>
                                <p><strong>⏱️ Horas:</strong> <%= actividad.horas_invertidas %> h</p>
                            </div>
                            
                            <div class="actividad-actions">
                                <a href="/actividades/edit/<%= actividad.id %>" class="btn-editar">✏️ Editar</a>
                                <a href="/actividades/delete/<%= actividad.id %>" 
                                   class="btn-eliminar" 
                                   onclick="return confirm('¿Estás seguro de que quieres eliminar esta actividad?')">🗑️ Eliminar</a>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } %>
        </section>
    </div>

    <script>
        document.getElementById('fecha').valueAsDate = new Date();
        
        const forms = document.querySelectorAll('form');
        forms.forEach(form => {
            form.addEventListener('submit', function(e) {
                const horas = document.getElementById('horas_invertidas');
                if (horas.value < 0 || horas.value > 24) {
                    e.preventDefault();
                    alert('Las horas invertidas deben estar entre 0 y 24');
                    horas.focus();
                }
            });
        });
    </script>
</body>
</html>